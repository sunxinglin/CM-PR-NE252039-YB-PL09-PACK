<UserControl x:Class="MPAssmebleRecipe.Apps.Views.UcOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MPAssmebleRecipe.Apps.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/" xmlns:viewmodels="clr-namespace:MPAssmebleRecipe.Apps.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:UcOperationViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 数据表格 -->
        <DataGrid ItemsSource="{Binding Logs}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              Grid.Row="0">
            <DataGrid.Columns>
                <!--<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>-->
                <DataGridTextColumn Header="时间" Binding="{Binding CreatTime, StringFormat='yyyy-MM-dd HH:ss:mm'}" Width="*"/>
                <DataGridTextColumn Header="姓名" Binding="{Binding UserName}" Width="*"/>
                <DataGridTextColumn Header="工号" Binding="{Binding EmployeeId}" Width="*"/>
                <DataGridTextColumn Header="操作" Binding="{Binding Operation}" Width="*"/>
                <DataGridTextColumn Header="页面" Binding="{Binding ViewName}" Width="*"/>
                <DataGridTextColumn Header="内容" Binding="{Binding Description}" Width="2*"/>
                <!--<DataGridTextColumn Header="工位" Binding="{Binding UserWorkstations}" Width="*"/>-->
                <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="*"/>
                <DataGridTextColumn Header="设备" Binding="{Binding DeviceInfo}" Width="*"/>
               
            </DataGrid.Columns>
        </DataGrid>

        <!-- 分页按钮 -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" Margin="10">
            <Button Content="首页" Command="{Binding FirstPageCommand}" Margin="5"/>
            <Button Content="上一页" Command="{Binding PrevPageCommand}" Margin="5"/>
            <TextBlock Text="{Binding CurrentPage}" Margin="5"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="/" Margin="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalPages}" Margin="5"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Content="下一页" Command="{Binding NextPageCommand}" Margin="5"/>
            <Button Content="尾页" Command="{Binding LastPageCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>

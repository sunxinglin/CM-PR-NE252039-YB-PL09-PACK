<UserControl
    x:Class="MPAssmebleRecipe.Apps.Views.CellManage.UcCellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="30"
    d:DesignWidth="120"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!--  极柱样式  -->
        <Style x:Key="PoleStyle" TargetType="Border">
            <Setter Property="Width" Value="25" />
            <Setter Property="Height" Value="25" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <!--  极性文本样式  -->
        <Style x:Key="PoleTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

        <!--  胶条样式T  -->
        <Style x:Key="TapeStyleT" TargetType="Rectangle">
            <Setter Property="Height" Value="5" />
            <Setter Property="Fill" Value="Purple" />
            <Setter Property="Margin" Value="0,2" />
        </Style>
        <!--  胶条样式L  -->
        <Style x:Key="TapeStyleL" TargetType="Rectangle">
            <Setter Property="Fill" Value="Purple" />
            <Setter Property="Margin" Value="0,2" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <!--  上方胶条  -->
        <Rectangle
            Grid.Column="1"
            Grid.ColumnSpan="3"
            Height="5"
            Margin="0,-5,0,0"
            VerticalAlignment="Top"
            Fill="Orange"
            Visibility="{Binding HasTopTape, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!--  左方胶条  -->
        <Rectangle
            Grid.Column="0"
            Margin="0,5,0,5"
            Fill="Orange"
            Visibility="{Binding HasLeftTape, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!--  电芯主体  -->
        <Border
            x:Name="CellBoder"
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Background="Black"
            BorderBrush="Blue"
            BorderThickness="6"
            CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <!--  左侧极柱  -->
                <Border
                    Grid.Column="1"
                    Background="{Binding LeftPoleBackground}"
                    Style="{StaticResource PoleStyle}">
                    <TextBlock
                        FontSize="40"
                        Foreground="{Binding LeftPoleForeground}"
                        Style="{StaticResource PoleTextStyle}"
                        Text="{Binding LeftPoleSymbol}" />
                </Border>

                <!--  右侧极柱  -->
                <Border
                    Grid.Column="4"
                    Background="{Binding RightPoleBackground}"
                    Style="{StaticResource PoleStyle}">
                    <TextBlock
                        FontSize="40"
                        Foreground="{Binding RightPoleForeground}"
                        Style="{StaticResource PoleTextStyle}"
                        Text="{Binding RightPoleSymbol}" />
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
<UserControl x:Class="UserTest.Views.AuthMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserTest.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="AuthMainViewMenu">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <ListView Style="{StaticResource MaterialDesignListView}" FontSize="15" FontWeight="Bold" ItemsSource="{Binding FuncList}" SelectedItem="{Binding SelectFun}"></ListView>
            <ContentControl prism:RegionManager.RegionName="UserRegion" Grid.Column="1"></ContentControl>
        </Grid>
    </Grid>
</UserControl>

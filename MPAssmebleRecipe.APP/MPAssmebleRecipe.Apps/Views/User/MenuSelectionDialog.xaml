<UserControl x:Class="UserTest.Views.MenuSelectionDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="547">
    
    <Grid Margin="16,16,-113,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,8">
            <CheckBox Content="全选"
                      IsChecked="{Binding IsAllSelected}"
                      Style="{StaticResource MaterialDesignCheckBox}"/>
        </StackPanel>

        <!-- 菜单列表 -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding MenuItems}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  MaterialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  MaterialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="选中" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                    HorizontalAlignment="Center"
                                      Width="auto"
                                    Style="{StaticResource MaterialDesignCheckBox}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="主页面" 
                                  Binding="{Binding MainPage}"
                                  Width="200"/>

                <DataGridTextColumn Header="按钮名称" 
                    Binding="{Binding Button}"
                    Width="180"/>
                
                <DataGridTextColumn Header="分类" 
                                  Binding="{Binding SubPage}"
                                  Width="120"/>
                
              
            </DataGrid.Columns>
        </DataGrid>

        <!-- 按钮栏 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,16,0,0">
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding CancelCommand}"
                    Content="取消"/>
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SaveCommand}"
                    Content="保存"
                    Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl> 
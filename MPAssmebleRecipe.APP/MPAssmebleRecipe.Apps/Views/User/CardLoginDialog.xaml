<UserControl x:Class="MPAssmebleRecipe.Apps.Views.CardLoginDialog"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             Width="300">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="刷卡登录"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" 
                    HorizontalAlignment="Center"
                    Margin="0,16">
            <MaterialDesign:PackIcon Kind="CreditCardScan"
                                   Width="100"
                                   Height="100"/>
            <TextBlock Text="请刷卡..."
                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                       HorizontalAlignment="Center"
                       Margin="0,8"/>
            <PasswordBox x:Name="tbx_CardNum" MaterialDesign:PasswordBoxAssist.Password="{Binding CardNumber, UpdateSourceTrigger=PropertyChanged}"
                       Style="{StaticResource MaterialDesignPasswordBox}"
                       PreviewKeyDown="PreviewKeyDownLogin"
                     Opacity="1"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,16,0,0">
            <Button Content="确认" Command="{Binding LoginCommand}"></Button>
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding CancelCommand}"
                    Content="取消"
                    Margin="8,0"/>
        </StackPanel>
    </Grid>
</UserControl>

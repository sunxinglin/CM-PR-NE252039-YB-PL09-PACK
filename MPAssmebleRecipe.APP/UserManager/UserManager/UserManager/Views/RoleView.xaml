<UserControl x:Class="UserTest.Views.RoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserTest.Views" 
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBlock.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    VerticalAlignment="Center" 
                    Margin="16,0">
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding AddRoleCommand}"
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <MaterialDesign:PackIcon Kind="AccountPlus" 
                                           VerticalAlignment="Center"/>
                    <TextBlock Text="添加角色" 
                             Margin="4,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding DeleteRoleCommand}"
                    CommandParameter="{Binding SelectedRole}"
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <MaterialDesign:PackIcon Kind="AccountRemove" 
                                           VerticalAlignment="Center"/>
                    <TextBlock Text="删除角色" 
                             Margin="4,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding EditMenusCommand}"
                    CommandParameter="{Binding SelectedRole}">
                <StackPanel Orientation="Horizontal">
                    <MaterialDesign:PackIcon Kind="PlaylistEdit" 
                                           VerticalAlignment="Center"/>
                    <TextBlock Text="编辑管理菜单" 
                             Margin="4,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- 主内容区 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧角色列表 -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="角色列表" 
                          Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                          Margin="16,8"/>
                
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Roles}"
                          SelectedItem="{Binding SelectedRole}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          MaterialDesign:DataGridAssist.CellPadding="13 8 8 8"
                          MaterialDesign:DataGridAssist.ColumnHeaderPadding="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="角色名称" 
                                          Binding="{Binding RoleName}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="描述" 
                                          Binding="{Binding Description}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="创建时间" 
                                          Binding="{Binding CreatedTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" 
                                          Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- 分隔线 -->
            <GridSplitter Grid.Column="1" 
                         Width="5" 
                         HorizontalAlignment="Center"
                         VerticalAlignment="Stretch"
                         Background="{DynamicResource MaterialDesignDivider}"/>

            <!-- 右侧菜单列表 -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="菜单列表" 
                          Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                          Margin="16,8"/>
                
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Menus}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          MaterialDesign:DataGridAssist.CellPadding="13 8 8 8"
                          MaterialDesign:DataGridAssist.ColumnHeaderPadding="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="主页面" 
                                          Binding="{Binding Page}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="子页面" 
                                          Binding="{Binding SubPage}" 
                                          Width="150"/>
                        <DataGridTextColumn Header="按钮" 
                                          Binding="{Binding ElementName}" 
                                          Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

<UserControl x:Class="UserTest.Views.LoginDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBlock.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Text="用户登录"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <!-- 登录内容 -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 用户名密码登录 -->
            <StackPanel Grid.Row="0" 
                        Visibility="{Binding IsPasswordLoginVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         MaterialDesign:HintAssist.Hint="用户名"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,8"/>
                
                <PasswordBox x:Name="PasswordBox"
                            Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                            MaterialDesign:HintAssist.Hint="密码"
                            Margin="0,8"/>
                
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        Content="登录"
                        Margin="0,16,0,0"/>
            </StackPanel>

            <!-- 刷卡登录 -->
            <StackPanel Grid.Row="1" 
                        Visibility="{Binding IsCardLoginVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,16,0,0">
                <TextBlock Text="请刷卡..."
                          Style="{StaticResource MaterialDesignBody1TextBlock}"
                          HorizontalAlignment="Center"/>
                
                <MaterialDesign:PackIcon Kind="CreditCardScan"
                                       Width="48"
                                       Height="48"
                                       HorizontalAlignment="Center"
                                       Margin="0,16"/>
            </StackPanel>
        </Grid>

        <!-- 切换登录方式 -->
        <Button Grid.Row="2"
                Style="{StaticResource MaterialDesignFlatButton}"
                Command="{Binding SwitchLoginModeCommand}"
                Content="{Binding SwitchButtonText}"
                Margin="0,16,0,0"
                Visibility="{Binding IsSwitchVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl> 
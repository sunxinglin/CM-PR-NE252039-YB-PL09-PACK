<UserControl x:Class="Ctp0600P.Client.UserControls.ScrewView.ScrewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ctp0600P.Client.UserControls.ScrewView"
             mc:Ignorable="d" 
             x:Name="ScrewViewCtl"
             d:DesignHeight="50" d:DesignWidth="50">

    <Grid Margin="1">
        <Rectangle x:Name="bkImgDoing" Fill="#0000" StrokeThickness="6"  Width="20" Height="20" Margin="-20" RadiusX="1" RadiusY="1">
            <Rectangle.Stroke >
                <SolidColorBrush x:Name="ReactStroke" Color="Orange" />
            </Rectangle.Stroke>
            <Rectangle.Style>
                <Style TargetType="Rectangle">
                    <Style.Triggers>
                        <Trigger Property="Visibility" Value="Visible">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames  Storyboard.TargetProperty ="(Rectangle.Stroke).Color" Duration="0:0:0.6" FillBehavior="HoldEnd" RepeatBehavior="Forever">
                                            <EasingColorKeyFrame KeyTime="0" Value="#0000"/>
                                            <EasingColorKeyFrame KeyTime="0:0:0.3" Value="Orange"/>
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                
            </Rectangle.Style>
              
        </Rectangle>

        <Rectangle x:Name="bkImgOK"  Fill="SkyBlue"  Width="20" Height="20"   Margin="-20"/>
        <Ellipse x:Name ="bkImgNG" Stroke="Red" StrokeThickness="5"  Fill="#0000" Width="20" Height="20"   Margin="-20"/>
        <TextBlock x:Name="txb_OrderNo" Foreground="Red" Panel.ZIndex="999" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2" FontSize="16" RenderTransformOrigin="0.5 0.5">
            <TextBlock.RenderTransform>
                <ScaleTransform ScaleX="{Binding ElementName=ScrewViewCtl, Path=TextScaleX}" ScaleY="{Binding ElementName=ScrewViewCtl, Path=TextScaleY}"></ScaleTransform>
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</UserControl>

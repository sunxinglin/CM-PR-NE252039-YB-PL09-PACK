<Window x:Class="Ctp0600P.Client.Views.Windows.SkipStepWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Ctp0600P.Client.Views.Windows"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="跳步"
        Width="800"
        Height="450"
        d:DataContext="{d:DesignInstance Type=local:SkipStepViewModel}"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="CheckBoxStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="False" />
            <Setter Property="FocusVisualStyle" Value="{DynamicResource CheckBoxFocusVisual}" />
            <Setter Property="Height" Value="52" />
            <Setter Property="IsChecked" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid Width="{TemplateBinding Height}" Height="{TemplateBinding Height}">
                                <Rectangle x:Name="CheckBoxRectangle"
                                           Fill="White"
                                           Opacity="0.3"
                                           RadiusX="5"
                                           RadiusY="5" />
                                <Rectangle x:Name="CheckBoxRectangleOut"
                                           RadiusX="5"
                                           RadiusY="5"
                                           Stroke="#FFABADB3"
                                           StrokeThickness="1" />
                                <Grid x:Name="CheckedMark"
                                      Width="{TemplateBinding Height}"
                                      Height="{TemplateBinding Height}"
                                      Visibility="Collapsed">
                                    <Viewbox>
                                        <Grid>
                                            <Path Data="M1,9 L10,17"
                                                  SnapsToDevicePixels="True"
                                                  Stroke="#3553ad"
                                                  StrokeThickness="3" />
                                            <Path Data="M8,17 L20,4"
                                                  SnapsToDevicePixels="True"
                                                  Stroke="#3553ad"
                                                  StrokeThickness="3" />
                                        </Grid>
                                    </Viewbox>
                                </Grid>
                            </Grid>
                            <ContentPresenter Grid.Column="1"
                                              Margin="8,0,0,0"
                                              VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckedMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <ListBox Margin="0"
                 HorizontalContentAlignment="Stretch"
                 VerticalContentAlignment="Top"
                 ItemContainerStyle="{StaticResource ListItemNullFocusContainerStyle}"
                 ItemsSource="{Binding _DataList.Value}"
                 ScrollViewer.CanContentScroll="True">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Height="auto"
                            Margin="5"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="LightBlue" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="CornerRadius" Value="2" />
                            </Style>
                        </Border.Style>
                        <Grid Margin="15,0,0,0">
                            <!--<Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>-->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="45" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="100" />
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0"
                                      Width="30"
                                      Height="30"
                                      Margin="0,0,0,0"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Center"
                                      Background="Red"
                                      FontSize="18"
                                      IsChecked="{Binding IsChecked, Mode=TwoWay, NotifyOnSourceUpdated=True}"
                                      IsEnabled="{Binding IsEnable, Mode=TwoWay}"
                                      Style="{StaticResource CheckBoxStyle}" />

                            <TextBlock Grid.Column="1"
                                       VerticalAlignment="Center"
                                       FontSize="18"
                                       Foreground="BlueViolet"
                                       Text="{Binding TaskName}" />
                            <TextBlock Grid.Column="2"
                                       MinWidth="120"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       FontSize="18"
                                       Text="{Binding Status, Mode=TwoWay}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Black" />
                                        <Setter Property="FontSize" Value="26" />
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="已完成">
                                                <Setter Property="Foreground" Value="DarkGreen" />
                                                <Setter Property="FontWeight" Value="Bold" />
                                            </Trigger>
                                            <Trigger Property="Text" Value="未完成">
                                                <Setter Property="Foreground" Value="Red" />
                                                <Setter Property="FontWeight" Value="Bold" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Command="{Binding Cancel}" Content="取消" />
            <Button Margin="50,0,100,0"
                    Background="LightGreen"
                    Command="{Binding AcceptSkip}"
                    Content="确定"
                    Foreground="Black" />
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="Ctp0600P.Client.UserControls.AGV.AGVBingPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:app="clr-namespace:Ctp0600P.Client"
        xmlns:viewModels="clr-namespace:Ctp0600P.Client.ViewModels"
        Height="300" Width="600"
        WindowStyle="ToolWindow" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen" 
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:AGVBingPageViewModel}"
        Title="绑定AGV" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition  Height="*"/>
            <RowDefinition  Height="*"/>
            <RowDefinition  Height="*"/>
            <RowDefinition  Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="AGV车号" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="0" Grid.Column="1"  FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="{Binding Source={x:Static app:App.Current},Path=AGVCode, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Grid.Row="1" Grid.Column="0" Content="Pack条码" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1"  FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="{Binding PackBarCode}" Width="272" IsReadOnly="False"/>

        <Label Grid.Row="2" Grid.Column="0" Content="总成码" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1"  FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="{Binding OutCode}" Width="272" IsReadOnly="False"/>

        <Grid Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <!--<Button  Content="绑定" Grid.Column="0" Command="{Binding BingAGV}" FontSize="18" MinWidth="200" MinHeight="50" FontWeight="Bold" HorizontalAlignment="Center"    IsEnabled="{Binding Enable}" VerticalAlignment="Center"/>-->
                <!--<Button  Content="绑定" Grid.Column="0" Click="Button_Click_BindingAGV" FontSize="18" MinWidth="200" MinHeight="50" FontWeight="Bold" HorizontalAlignment="Center"    IsEnabled="{Binding Enable}" VerticalAlignment="Center"/>-->
                <Button Grid.Column="0" Grid.ColumnSpan="2"  Content="取消" Click="Button_Click" FontSize="18" MinWidth="200" MinHeight="50" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <!--<Button Grid.Column="0"  Content="扫码1" Command = "{Binding scan_1}"  FontSize="18" MinWidth="200" MinHeight="50" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>-->
                <!--<Button Grid.Column="1"  Content="扫码2" Command = "{Binding scan_2}"  FontSize="18" MinWidth="200" MinHeight="50" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>-->
            </Grid>

        </Grid>

    </Grid>
</Window>
